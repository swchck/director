# 01 — Basic CRUD

Demonstrates reading and writing items to a Directus collection using the typed `Items[T]` wrapper.

**What you'll learn:**
- Creating a Directus client
- Defining a Go struct matching a Directus collection
- Creating, listing, filtering, updating, and deleting items

```bash
task up
go run example/01-basic-crud/main.go
```
